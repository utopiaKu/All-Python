per = float(input(" Enter the peccentage marks secured : "))

if per >= 90:
  print("Grade is A+")
if per >= 80 and per < 90:
  print("Grade is A")
if per >= 60 and per < 80:
  print("Grade is B")
if per >= 50 and per < 60:
  print("Grade is C")
if per < 50:
  print("Grade is D")

